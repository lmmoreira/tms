receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318
  rabbitmq:
    endpoint: http://tms-broker:15672
    username: tms-otel
    password: bitnami
    collection_interval: 10s
  nginx:
    endpoint: "http://tms-nginx/nginx_status"
    collection_interval: 10s
  filelog:
    include: ["/var/log/tms/*.json", "/var/log/nginx/access.log"]
    operators:
      - type: json_parser
        if: 'body matches "^{.*}$"'
        parse_from: body

processors:
  batch:
  attributes:
    actions:
      - key: Source
        value: "Otel-Collector"
        action: upsert

extensions:
  health_check:
  pprof:
  zpages:

exporters:
  #prometheus:
  #  endpoint: "0.0.0.0:8889"
  #  namespace: default
  prometheusremotewrite:
    endpoint: "http://tms-prometheus:9090/api/v1/write"
    tls:
      insecure: true
  debug:
    verbosity: detailed
  loki:
    endpoint: "http://tms-loki:3100/loki/api/v1/push"
    default_labels_enabled:
      exporter: true
      job: true

service:
  extensions: [health_check, pprof, zpages]
  pipelines:
    metrics:
      receivers: [otlp, rabbitmq, nginx]
      processors: [batch]
      exporters: [debug, prometheusremotewrite]
    logs:
      receivers: [filelog]
      processors: [attributes]
      exporters: [loki]